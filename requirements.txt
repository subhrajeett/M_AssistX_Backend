# requirements.txt – Manufacturing Downtime Agent
# Install with: pip install -r requirements.txt
awscli 
# AWS Bedrock AgentCore SDK (runtime + memory)
bedrock-agentcore>=1.4.8
bedrock-agentcore-starter-toolkit>=0.1.0

# Strands Agents framework (LLM orchestration + tool use)
strands-agents>=0.1.0
strands-agents-tools>=0.1.0    # optional – only needed if you want built-in tools

# AWS SDK
boto3>=1.35.0
botocore>=1.35.0

# Data & visualisation
matplotlib>=3.9.0

# Utilities
python-dotenv>=1.0.0           # optional – load .env file for local dev


# Manufacturing Agent — Python dependencies
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# MSSQL connection
pyodbc>=5.1.0
